import { ConditionToImgPipe } from './condition-to-img.pipe';

describe('ConditionToImgPipe', () => {
  it('create an instance', () => {
    const pipe = new ConditionToImgPipe();
    expect(pipe).toBeTruthy();
  });
});
